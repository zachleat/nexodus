{
  "name": "@zachleat/nexodus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "import": "./import.sh",
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --quiet",
    "production": "./import.sh persist && npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/import": "^1.0.0"
  }
}
